import { StoreProvider } from './ui/StoreProvider';
import { AppDispatch, createStore } from './config/store';
import { StateSchema, ThunkConfig } from './config/StateSchema';

export {
  createStore,
  StoreProvider,
  AppDispatch,
  StateSchema,
  ThunkConfig,
};
